# Archronis
1)Код к основному заданию находится в ветке Basic_task.
2)Кодировщик по алгоритму хаффмана находится в ветке Huffman_arch (Интерфейс отличен от задания. 
Данный код архивирует только на алфавитах малой мощности но интересенн с точки зрения реализации алгоритма)
3)LZW_trie - попытка реализовать LZW бором а не хеш-таблицей. Была допущена алгоритмическая ошибка.
Дебажить деревья трудно, поэтому так и остался незавершенным
4)LZW_Client-Server - Вполне рабочая реализация класса Socket. На тестовой паре клиент-сервер работает и архивирует.
Но при потытке вклинить в это класс Server с очередью и потоками(и даже просто однопоточный) отказывается работать
В последний день дописать не успел.


Так вышло, что в ветке master оказался вообще весь код написанный за это время. Потом раскидал на отдельные ветки
